/// <reference types="cypress" />

describe('Countdown Timer', () => {
  beforeEach(() => {
    cy.visit('/')
  });

  it('The timer should count down correctly', () => {
      cy.log("test")
  });



});
